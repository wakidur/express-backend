extends layout

block content
  h1= title
  p Welcome to #[em LocalLibrary], a very basic Express website developed as a tutorial example on the Mozilla Developer Network.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:
    div(class="row")
      div(class="col-sm-4")
        ul(class='list-group')
          li(class='list-group-item') #[strong Books:] !{data.book_count}
          li(class='list-group-item') #[strong Copies:] !{data.book_instance_count}
          li(class='list-group-item') #[strong Copies available:] !{data.book_instance_available_count} 
          li(class='list-group-item') #[strong Authors:] !{data.author_count}
          li(class='list-group-item') #[strong Genres:] !{data.genre_count}